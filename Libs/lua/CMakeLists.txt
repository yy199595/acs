
CMAKE_MINIMUM_REQUIRED(VERSION 3.0.0)
PROJECT(lua)


INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src)

AUX_SOURCE_DIRECTORY(./src SUPER_SRC)
INCLUDE_DIRECTORIES(./src)

ADD_LIBRARY(lua STATIC ${SUPER_SRC})
