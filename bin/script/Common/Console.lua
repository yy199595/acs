---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by leyi.
--- DateTime: 2023/5/8 20:17
---
local Console = { }

Console.LogDebug = 1
Console.LogInfo = 2
Console.LogWarn = 3
Console.LogError = 4
Console.LogFatal = 5

local debug_module = os.debug
local lfmt = require("util.fmt")
local string_format = lfmt.format
local console = require("core.log")

function Console.Show(type, fmt, ...)
    if debug_module then
        console.Show(type, fmt, ...)
    end
end

function Console.Info(fmt, ...)
    if debug_module then
        local message = string_format(fmt, ...)
        console.Show(Console.LogInfo, message)
    end
end

function Console.Debug(fmt, ...)
    if debug_module then
        local message = string_format(fmt, ...)
        console.Show(Console.LogDebug, message)
    end
end

function Console.Warning(fmt, ...)
    if debug_module then
        local message = string_format(fmt, ...)
        console.Show(Console.LogWarn, message)
    end
end

function Console.Error(fmt, ...)
    if debug_module then
        local message = string_format(fmt, ...)
        console.Show(Console.LogError, message)
    end
end

function Console.Fatal(fmt, ...)
    if debug_module then
        local message = string_format(fmt, ...)
        console.Show(Console.LogFatal,message)
    end
end

return Console